import 'package:flutter/material.dart';

class EmotionBuilder extends StatelessWidget {
  const EmotionBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
